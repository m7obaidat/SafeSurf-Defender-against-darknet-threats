# SQL Queries Used in SafeSurf Application

## SELECT Queries (Read Operations)
1. User Authentication:
   - User.query.filter_by(username=username).first()
   - User.query.filter_by(email=email).first()

2. User Management:
   - User.query.get(int(user_id))
   - User.query.all()
   - User.query.count()

3. ML Model Management:
   - MLModel.query.order_by(MLModel.timestamp.desc()).all()

## INSERT/UPDATE Queries (Write Operations)
1. User Operations:
   - db.session.add(user)
   - db.session.commit()

2. ML Model Operations:
   - db.session.add(ml_model)
   - db.session.commit()

## Database Management Queries
1. Schema Management:
   - db.create_all()
   - db.drop_all()

2. Session Management:
   - db.session.close()
   - db.session.rollback()

## Query Usage Context

### User Authentication
- Used in login route
- Used in registration validation
- Used in profile management

### User Management
- Used in admin dashboard
- Used in user listing
- Used in user profile retrieval

### ML Model Management
- Used in model training
- Used in model listing
- Used in model approval process

### Database Operations
- Used in application initialization
- Used in error handling
- Used in transaction management

## Total Count
- Unique SELECT queries: 6
- Unique INSERT/UPDATE queries: 3
- Unique Database Management queries: 4
- Total unique queries: 13

## Most Frequently Used Queries
1. db.session.commit()
2. User.query.filter_by()
3. db.session.add()
4. User.query.all()

## Database Configuration
- Database Type: SQLite
- Connection String: sqlite:///users.db
- ORM: SQLAlchemy 